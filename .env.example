# =============================================================================
# SLOE FIT - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# AI Provider Configuration (Server-side only - secure)
# -----------------------------------------------------------------------------
# Choose your AI provider: openai | anthropic | google
AI_PROVIDER=openai

# Option 1: Unified API key (works with any provider)
AI_API_KEY=your-api-key-here

# Option 2: Provider-specific keys (alternative to AI_API_KEY)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_AI_API_KEY=...

# -----------------------------------------------------------------------------
# USDA FoodData Central API (Optional - for accurate nutrition lookups)
# -----------------------------------------------------------------------------
# Get your free API key at: https://fdc.nal.usda.gov/api-key-signup.html
VITE_USDA_API_KEY=your-usda-api-key-here

# -----------------------------------------------------------------------------
# Shopify Configuration (Optional - for e-commerce features)
# -----------------------------------------------------------------------------
# Note: Storefront access tokens are safe for client-side use
VITE_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
VITE_SHOPIFY_STOREFRONT_ACCESS_TOKEN=your-storefront-access-token
# Product IDs from Shopify Admin
VITE_SHOPIFY_CREATINE_ID=gid://shopify/Product/123456789
VITE_SHOPIFY_PRE_WORKOUT_ID=gid://shopify/Product/987654321

# -----------------------------------------------------------------------------
# Sentry Configuration (Optional - for error tracking)
# -----------------------------------------------------------------------------
# Get your DSN from: https://sentry.io/
VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# -----------------------------------------------------------------------------
# Upstash Redis Configuration (Optional - for persistent rate limiting)
# -----------------------------------------------------------------------------
# Get credentials from: https://console.upstash.com/
# Without these, rate limiting falls back to in-memory (resets on cold starts)
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxx

# -----------------------------------------------------------------------------
# Inngest Configuration (Optional - for reliable AI job processing)
# -----------------------------------------------------------------------------
# Get keys from: https://app.inngest.com/
INNGEST_EVENT_KEY=xxx
INNGEST_SIGNING_KEY=xxx

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# Local Development:
#   1. Copy this file to .env
#   2. Fill in your values
#   3. Run: npx vercel dev (recommended for API routes)
#      OR: npm run dev (frontend only, AI features won't work)
#
# Vercel Deployment:
#   1. Add all variables to Vercel Dashboard > Settings > Environment Variables
#   2. AI_PROVIDER and API keys go in the Environment Variables (not in code)
#   3. VITE_* variables are embedded in the build (safe for public values only)
#
# Switching AI Providers:
#   1. Change AI_PROVIDER to: openai, anthropic, or google
#   2. Set the corresponding API key
#   3. Redeploy - no code changes needed!
#
# =============================================================================
